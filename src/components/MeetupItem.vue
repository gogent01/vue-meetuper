<template>
  <div class="column is-one-third">
<!--    <router-link :to="'/meetups/' + meetup._id" class="card large">-->
    <div class="card large is-hoverable">
      <router-link :to="{name: 'PageMeetupDetail', params: {id: meetup._id}}" class="card-meetup-link">
        <div class="card-image">
          <figure class="image is-4by3">
            <img :src="meetup.image" alt="Image">
          </figure>
        </div>
        <div class="card-content">
          <div class="level m-b-md">
            <div class="media-left">
              <div class="level-item has-text-centered">
                <div>
                  <p class="title">{{ meetup.startDate | formatDate('MMM')}}</p>
                  <p class="title">{{ meetup.startDate | formatDate('D')}}</p>
                </div>
              </div>
            </div>
            <div class="media-content">
              <p class="title is-4 no-padding is-marginless">{{ meetup.title }}</p>
              <!-- <p><span class="title is-6"><a href="http://twitter.com/#">@twitterid</a></span></p> -->
              <span class="tag is-success">{{ meetup.category.name | capitalize }}</span>
              <p class="subtitle is-7">{{ meetup.location }}</p>
            </div>
          </div>
          <div class="content">
            {{ meetup.description }}
            <div class="background-icon"><span class="icon-twitter"></span></div>
          </div>
        </div>
      </router-link>
    </div>
  </div>
</template>

<script>
  export default {
    props: {
      meetup: {
        type: Object,
        require: true
      }
    }
  }
</script>

<style lang="scss" scoped>
  .card-meetup-link {
    color: #000000;
  }

  .is-hoverable {
    transition: 0.2s;

    &:hover {
      -webkit-box-shadow: 4px 4px 11px -1px rgba(80, 80, 80, 0.43);
      -moz-box-shadow: 4px 4px 11px -1px rgba(80, 80, 80, 0.43);
      box-shadow: 4px 4px 11px -1px rgba(80, 80, 80, 0.43);
    }
  }
</style>